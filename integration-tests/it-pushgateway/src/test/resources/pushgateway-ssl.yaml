tls_server_config:
  # cert and key have been generated with the following command:
  # openssl req -x509 -newkey rsa:4096 -keyout key.pem -out
  # cert.pem -sha256 -days 36500 -nodes -subj "/CN=localhost"
  cert: |
    -----BEGIN CERTIFICATE-----
    secret key
    -----END CERTIFICATE-----
  key: |
    -----BEGIN PRIVATE KEY-----
    secret key
    -----END PRIVATE KEY-----
